# Node.js + Express + PostgreSQL CRUD API (CMP464 Project 4)

CRUD REST API for `/api/users` using Node.js, Express and PostgreSQL.
Uses `.env` for all DB credentials and `.gitignore` to keep `.env` out of GitHub.
Includes SQL schema + seed files and works with local Postgres or Neon (`DATABASE_URL`).
